# 七年级上册知识图谱构建器指南

## 概述

七年级上册知识图谱构建器是一个专门处理七年级上册数学教材内容的工具，它可以从数学教材文件中自动识别数学概念、公式、定理等实体，并抽取它们之间的关系，构建结构化的知识图谱。

## 功能特点

- **专门处理七年级上册**: 只处理文件名以 `7.` 开头的文件（如 `7.1.docx`, `7.2.docx` 等）
- **实体识别**: 自动识别数学概念、公式、定理、例题、解题方法等
- **关系抽取**: 抽取实体之间的推导、应用、包含等关系
- **数据去重**: 自动去除重复的实体和关系
- **结果保存**: 将处理结果保存为JSON格式文件
- **Neo4j支持**: 可导入到Neo4j图数据库进行可视化查询

## 文件要求

### 数据目录
数据文件应存放在 `D:\数据` 目录下（可在 `config.py` 中修改）

### 文件命名格式
七年级上册文件应采用以下命名格式：
- `7.1.docx` - 七年级上册第1章
- `7.2.docx` - 七年级上册第2章
- `7.3.docx` - 七年级上册第3章
- ...以此类推

## 使用方法

### 方法一：使用批处理文件（推荐）
双击运行 `start_grade7.bat` 文件

### 方法二：命令行运行
```bash
cd knowledge_graph_project
python grade7_builder.py
```

### 方法三：在Python中调用
```python
from grade7_builder import Grade7KnowledgeGraphBuilder

builder = Grade7KnowledgeGraphBuilder()
entities, relations = builder.build_grade7_knowledge_graph()
```

## Neo4j数据导入

处理完成后，可以将结果导入到Neo4j数据库：

### 方法一：使用批处理文件（推荐）
双击运行 `start_neo4j_import.bat` 文件

### 方法二：命令行运行
```bash
cd knowledge_graph_project
python neo4j_importer.py
```

### 导入工具功能
- **导入数据**: 将七年级上册处理结果导入Neo4j
- **清空数据库**: 清空Neo4j中的所有数据
- **导出数据**: 将Neo4j中的数据导出为JSON文件
- **统计查询**: 查看数据库中的实体和关系统计信息

## 输出文件

处理完成后，结果将保存在 `./output/grade7/` 目录下：

- `entities.json` - 所有识别的实体数据
- `relations.json` - 所有抽取的关系数据  
- `statistics.json` - 处理统计信息

## 配置说明

主要配置在 `config.py` 文件中：

- `DATA_DIR = "D:\\数据"` - 数据文件目录
- `OUTPUT_DIR = "./output"` - 输出目录
- Neo4j数据库连接配置（可选）

## 注意事项

1. **文件大小限制**: 单个文件超过50,000字符时只处理前50,000字符
2. **处理时间**: 根据文件数量和大小，处理可能需要较长时间
3. **Neo4j连接**: 如果Neo4j不可用，程序会自动进入离线模式
4. **模型依赖**: 需要BERT中文模型进行实体识别

## 故障排除

### 数据目录不存在
错误信息：`数据目录不存在: D:\数据`
解决方法：创建数据目录或将 `config.py` 中的 `DATA_DIR` 修改为正确的路径

### 缺少依赖库
错误信息：`ModuleNotFoundError`
解决方法：运行 `pip install -r requirements.txt`

### Neo4j连接失败
警告信息：`Neo4j连接失败`
解决方法：检查Neo4j服务是否启动，或修改 `config.py` 中的连接配置

## 示例输出

处理完成后会显示类似以下信息：
```
七年级上册知识图谱构建完成！
识别到 1250 个实体
抽取到 890 个关系
去重后: 980 个实体, 650 个关系
七年级上册结果已保存到 ./output/grade7 目录
```

## 技术支持

如有问题，请检查：
1. 数据文件是否存在于正确目录
2. 文件命名是否符合要求（7.1, 7.2等）
3. 必要的Python依赖库是否安装
4. Neo4j服务是否正常运行（如需要使用数据库功能）